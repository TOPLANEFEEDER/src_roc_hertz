`cleanShape` allows to reduce the face number and remove the edges witch are not necessary.

```
USAGE:

   ./cleanShape  <string> [--]

Where:

   -i <string>,  --input <string>
     (required)  Name of the input shp file
```
